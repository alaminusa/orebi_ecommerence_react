
const Label = ({labelName, className }) => {
  return (
 
        <label className={`${className} font-dm text-base font-bold text-primaryColor block`}>{labelName}</label>
   
  )
}

export default Label